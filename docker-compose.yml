version: '3'
services:
  nginx:
    image: nginx
    container_name: nginx
    restart: always
    ports:
      - 80:80
    privileged: true
    volumes:
      - './nginx/conf.d:/etc/nginx/conf.d'
  mysql:
    restart: always
    image: mysql
    container_name: mysql
    ports:
      - 3306:3306
